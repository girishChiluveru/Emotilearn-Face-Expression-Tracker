# Frontend environment variables — copy to .env for development
# Vite exposes VITE_* vars to the browser bundle

# Express Node.js API base URL
VITE_API_URL=http://localhost:3000

# Socket.io server URL (usually same as API)
VITE_WS_URL=http://localhost:3000
